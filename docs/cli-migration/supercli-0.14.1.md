# SuperCLI 0.14.1 connector CLI migration

<!-- Generated by plugins/ericsson-connector-cli/scripts/build_migration_docs.py. -->

> **Existing SuperCLI scripts are not drop-in compatible.** Translate each
> command with this reviewed map; do not carry URLs, credentials, raw-output
> escape hatches, profile selectors, or throttling overrides onto argv.

This guide is generated from `supercli-0.14.1.yaml`, the machine-readable
migration authority. It covers only the pinned evidence inventory and does
not infer commands or flags that were absent from that evidence.

## Pinned source

- SuperCLI version: `0.14.1`
- Commit: `6645cd0bb56cc54aa4f1d49095490832c9528dbb`
- Binary SHA-256: `72ce9d9ad14b451b53a7f0f06786d75336a302562a8ed6d0dbafc2cb7657cc6a`
- Extracted service commands: `108` (Jira 24, GitLab 46, Confluence 19, ARM 19)

The evidence filenames are recorded without developer-local paths:
`SUPER-CLI-ARCHITECTURE.md`, `PLUGIN-GAP-ANALYSIS.md`, and
`out/func-strings.txt`.

## Quick start

The facade and `--help` are always visible. Execution uses the active profile,
and each standalone connector must be enabled and configured there.
Never place connector origins, tokens, passwords, certificates, or profile
selection on the command line.

```bash
otto plugins enable ericsson-jira
otto jira issue get ERIC-123

loop24 plugins enable ericsson-gitlab
loop24 gitlab pipeline view group/project 918
```

Reads are model-free and deterministic:

```bash
otto jira issue get ERIC-123 --json
loop24 gitlab pipeline view group/project 918 --json
```

Writes require exactly one intent flag:

```bash
otto confluence page update 12345 --body-file page.md --dry-run
loop24 gitlab pipeline retry group/project 918 --confirm
```

`--dry-run` previews without mutation. `--confirm` authorizes one exact
invocation. A confirmed write with an uncertain remote outcome returns
`write_ambiguous`; it is never silently retried.

## Stable output and exit codes

`--json` emits exactly one `ericsson.connector-cli/v1` envelope on stdout:

```json
{"connector":"jira","data":{},"meta":{},"mode":"read","ok":true,"operation":"jira_get_issue","schema_version":"ericsson.connector-cli/v1","warnings":[]}
```

- Exit code 0: successful read, preview, or confirmed write.
- Exit code 2: usage, schema, local input, or mutation-intent error.
- Exit code 3: connector disabled, unsupported, unavailable, or not configured.
- Exit code 4: classified remote or connector operation failure.
- Exit code 5: `write_ambiguous`; reconcile remote state before another write.

## Disposition summary

| Disposition | Rows |
|---|---:|
| `equivalent` | 7 |
| `renamed` | 19 |
| `safer-different` | 32 |
| `not-yet-supported` | 31 |
| `no-equivalent` | 19 |

`equivalent` means the same bounded user outcome; `renamed` changes
command grammar; `safer-different` preserves the outcome with deliberate
profile, input, output, write-intent, or ambiguity controls. Unsupported
rows are either release gaps or deliberate exclusions.

## Jira

### equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli jira field list` | `{brand} jira field list` | `jira_list_fields` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The bounded field inventory provides the same discovery outcome. Evidence: out/func-strings.txt:3110; PLUGIN-GAP-ANALYSIS.md section 4 |

### renamed

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli jira issue list` | `{brand} jira issue search --jql <jql> --max-results <count>` | `jira_search_issues` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Jira issue listing is exposed as bounded JQL search. Evidence: out/func-strings.txt:3025; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue transition list` | `{brand} jira transition list <key>` | `jira_list_transitions` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The same discovery outcome is available under the curated transition tree. Evidence: out/func-strings.txt:3042; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue view` | `{brand} jira issue get <key>` | `jira_get_issue` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | A bounded issue projection replaces the source view spelling. Evidence: out/func-strings.txt:3030; SUPER-CLI-ARCHITECTURE.md sections 5.3 and 6.3 |
| `super-cli jira link-type list` | `{brand} jira link-type list` | `jira_list_link_types` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The connector exposes the same bounded link-type discovery outcome. Evidence: out/func-strings.txt:3085; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira project view` | `{brand} jira project get <project>` | `jira_get_project` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The curated get command returns bounded project discovery metadata. Evidence: out/func-strings.txt:3113; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira user search` | `{brand} jira user search-assignable <project> --query <text>` | `jira_search_assignable_users` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The connector deliberately limits results to users assignable in one project. Evidence: out/func-strings.txt:3088; PLUGIN-GAP-ANALYSIS.md section 4 |

### safer-different

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli jira component list` | `{brand} jira project get <project>` | `jira_get_project` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Project lookup returns bounded issue types, components, and versions together. Evidence: out/func-strings.txt:3094; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli jira issue assign` | `{brand} jira issue assign <key> <assignee> --dry-run\|--confirm` | `jira_assign_issue` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Assignment uses one bounded assignee and genuine one-shot write intent. Evidence: out/func-strings.txt:3049; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue comment add` | `{brand} jira issue comment <key> --body-file <path> --dry-run\|--confirm` | `jira_add_comment` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Comment text comes from a bounded file instead of process arguments and preserves ambiguous-write refusal. Evidence: out/func-strings.txt:3038; PLUGIN-GAP-ANALYSIS.md sections 2 and 3 |
| `super-cli jira issue create` | `{brand} jira issue create <project> <issue-type> --summary <text> --dry-run\|--confirm` | `jira_create_issue` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Creation accepts a bounded issue shape and rejects arbitrary raw Jira field JSON. Evidence: out/func-strings.txt:3063; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue edit` | `{brand} jira issue update <key> --field <name=value> --dry-run\|--confirm` | `jira_update_fields` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Repeatable --field entries are bounded by an allowlist; arbitrary raw JSON mutation is not accepted. Evidence: out/func-strings.txt:3066; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue label add` | `{brand} jira issue label <key> add <label> --dry-run\|--confirm` | `jira_manage_labels` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Label addition is a bounded explicit operation with protected write authority. Evidence: out/func-strings.txt:3070; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue label remove` | `{brand} jira issue label <key> remove <label> --dry-run\|--confirm` | `jira_manage_labels` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Label removal is a bounded explicit operation with protected write authority. Evidence: out/func-strings.txt:3074; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue link` | `{brand} jira issue link <inward-key> <outward-key> <link-type> --dry-run\|--confirm` | `jira_link_issues` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Linking preserves direction and refuses to retry an uncertain mutation. Evidence: out/func-strings.txt:3078; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue transition do` | `{brand} jira issue transition <key> <transition-id> --dry-run\|--confirm` | `jira_transition_issue` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Transition execution accepts the discovered transition id and explicit intent. Evidence: out/func-strings.txt:3045; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue-type list` | `{brand} jira project get <project>` | `jira_get_project` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Project lookup returns bounded issue types together with components and versions. Evidence: out/func-strings.txt:3082; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli jira version list` | `{brand} jira project get <project>` | `jira_get_project` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Project lookup returns bounded versions together with issue types and components. Evidence: out/func-strings.txt:3097; PLUGIN-GAP-ANALYSIS.md section 4 |

### not-yet-supported

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli jira board list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Jira Agile board enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:3099; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira issue comment list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Issue lookup includes a bounded comment projection, but there is no standalone paginated comment-list contract. Evidence: out/func-strings.txt:3034; PLUGIN-GAP-ANALYSIS.md section 1.1 |
| `super-cli jira priority list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Standalone priority enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:3091; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira project list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | The connector resolves one project but does not enumerate all visible projects. Evidence: out/func-strings.txt:3053; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira sprint list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Jira Agile sprint enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:3103; SUPER-CLI-ARCHITECTURE.md section 6.3 |
| `super-cli jira sprint move` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Moving issues into sprints has no reviewed connector preview and confirmation path. Evidence: out/func-strings.txt:3106; SUPER-CLI-ARCHITECTURE.md section 6.3 |

### no-equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| — | — | — | — | — | No rows in this disposition. |

## GitLab

### equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli gitlab commit list` | `{brand} gitlab commit list <project>` | `gitlab_list_commits` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The bounded commit listing provides the same read outcome. Evidence: out/func-strings.txt:2959; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab job log` | `{brand} gitlab job log <project> <job-id>` | `gitlab_job_log` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The connector returns a bounded, truncated-safe job log. Evidence: out/func-strings.txt:2935; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab pipeline list` | `{brand} gitlab pipeline list <project>` | `gitlab_list_pipelines` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The bounded pipeline inventory provides the same read outcome. Evidence: out/func-strings.txt:2914; SUPER-CLI-ARCHITECTURE.md section 6.5 |

### renamed

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli gitlab commit view` | `{brand} gitlab commit show <project> <sha>` | `gitlab_read_commit` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Commit details are exposed under the curated show spelling. Evidence: out/func-strings.txt:2939; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab file list` | `{brand} gitlab repository tree <project> --ref <ref> --path <path>` | `gitlab_list_repository_tree` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Repository-tree listing provides the bounded file and directory discovery outcome. Evidence: out/func-strings.txt:2965; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab file view` | `{brand} gitlab file show <project> <path> --ref <ref>` | `gitlab_read_file` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | File content is bounded by --max-bytes and exposed under the show spelling. Evidence: out/func-strings.txt:2962; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab group project list` | `{brand} gitlab group project-list <group>` | `gitlab_list_group_projects` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Bounded group traversal replaces the source command's spaced project/list path. Evidence: out/func-strings.txt:3006; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab mr approval list` | `{brand} gitlab mr approval-show <project> <iid>` | `gitlab_merge_request_approvals` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Approval state is exposed under a curated read-only spelling. Evidence: out/func-strings.txt:2893; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab mr commit list` | `{brand} gitlab mr commit-list <project> <iid>` | `gitlab_list_merge_request_commits` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The bounded MR commit inventory uses a curated hyphenated leaf. Evidence: out/func-strings.txt:2890; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab mr note list` | `{brand} gitlab mr discussion-list <project> <iid>` | `gitlab_list_merge_request_discussions` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The connector returns bounded discussions and notes together. Evidence: out/func-strings.txt:2873; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab mr view` | `{brand} gitlab mr show <project> <iid>` | `gitlab_read_merge_request` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Bounded MR details are exposed under the show spelling. Evidence: out/func-strings.txt:2865; SUPER-CLI-ARCHITECTURE.md section 6.5 |

### safer-different

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli gitlab job play` | `{brand} gitlab job play <project> <job-id> --dry-run\|--confirm` | `gitlab_play_job` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Manual-job execution uses explicit one-shot intent and preserves ambiguous-write refusal. Evidence: out/func-strings.txt:2999; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab job retry` | `{brand} gitlab job retry <project> <job-id> --dry-run\|--confirm` | `gitlab_retry_job` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Retry is authorized once and does not retry an unproved mutation outcome. Evidence: out/func-strings.txt:2995; PLUGIN-GAP-ANALYSIS.md sections 2 and 4 |
| `super-cli gitlab mr approve` | `{brand} gitlab mr approve <project> <iid> --sha <sha> --dry-run\|--confirm` | `gitlab_approve_merge_request` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Approval supports an exact SHA precondition and one-shot write authority. Evidence: out/func-strings.txt:2897; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab mr create` | `{brand} gitlab mr create <project> <source-branch> <target-branch> --title <text> --dry-run\|--confirm` | `gitlab_create_merge_request` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | MR creation uses bounded fields and refuses ambiguous mutation retries. Evidence: out/func-strings.txt:2904; PLUGIN-GAP-ANALYSIS.md sections 1.2 and 2 |
| `super-cli gitlab mr discussion resolve` | `{brand} gitlab mr discussion-resolve <project> <iid> <discussion-id> --resolved --dry-run\|--confirm` | `gitlab_resolve_discussion` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Resolution is explicit, bounded to one discussion, and protected by write intent. Evidence: out/func-strings.txt:2882; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab mr discussion unresolve` | `{brand} gitlab mr discussion-resolve <project> <iid> <discussion-id> --no-resolved --dry-run\|--confirm` | `gitlab_resolve_discussion` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Unresolve is represented by the same bounded operation with resolved=false. Evidence: out/func-strings.txt:2886; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab mr edit` | `{brand} gitlab mr update <project> <iid> --title <text> --dry-run\|--confirm` | `gitlab_update_merge_request` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | MR updates accept only reviewed bounded fields and explicit intent. Evidence: out/func-strings.txt:2907; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab mr list` | `{brand} gitlab mr list <project>` | `gitlab_list_merge_requests` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The replacement requires a project and does not preserve SuperCLI's user-scoped no-project mode. Evidence: out/func-strings.txt:2861; post-Wave-3 reconciliation |
| `super-cli gitlab mr merge` | `{brand} gitlab mr merge <project> <iid> --sha <sha> --dry-run\|--confirm` | `gitlab_merge_merge_request` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Merge preserves approval, SHA, pipeline, and ambiguous-write protections. Evidence: out/func-strings.txt:2901; PLUGIN-GAP-ANALYSIS.md sections 2 and 4 |
| `super-cli gitlab mr note add` | `{brand} gitlab mr note <project> <iid> --body-file <path> --dry-run\|--confirm` | `gitlab_create_mr_note` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Note content comes from a bounded file and the mutation uses one-shot authority. Evidence: out/func-strings.txt:2876; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab mr note reply` | `{brand} gitlab mr discussion-reply <project> <iid> <discussion-id> --body-file <path> --dry-run\|--confirm` | `gitlab_reply_to_discussion` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Replies are bound to one discussion and use bounded file input and explicit intent. Evidence: out/func-strings.txt:2879; PLUGIN-GAP-ANALYSIS.md section 4 |
| `super-cli gitlab pipeline retry` | `{brand} gitlab pipeline retry <project> <pipeline-id> --dry-run\|--confirm` | `gitlab_retry_pipeline` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Retry is one-shot, preserves pipeline reconciliation, and never replays an ambiguous write. Evidence: out/func-strings.txt:2924; PLUGIN-GAP-ANALYSIS.md sections 2 and 4 |
| `super-cli gitlab pipeline view` | `{brand} gitlab pipeline view <project> <pipeline-id>` | `gitlab_read_pipeline` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The replacement returns only bounded normalized pipeline metadata for the evidenced project and pipeline inputs. Evidence: out/func-strings.txt:2917; post-Wave-3 reconciliation |
| `super-cli gitlab project view` | `{brand} gitlab project resolve <project>` | `gitlab_resolve_project` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The connector resolves one exact project and returns bounded identity metadata. Evidence: out/func-strings.txt:2975; SUPER-CLI-ARCHITECTURE.md section 6.5 |

### not-yet-supported

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli gitlab branch list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Branch enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2943; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab job cancel` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Job cancellation has no reviewed preview and confirmation path in this release. Evidence: out/func-strings.txt:2992; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab job view` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | A standalone normalized job-detail operation is not exposed in this release. Evidence: out/func-strings.txt:2932; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab mr diff` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | The connector reads MR metadata but does not expose the source command's per-file diff contract. Evidence: out/func-strings.txt:2869; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab mr pipeline list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | MR-specific pipeline enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2984; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab mr rebase` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | MR rebase has no reviewed non-mutating preview and confirmation path in this release. Evidence: out/func-strings.txt:2910; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab pipeline cancel` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Pipeline cancellation has no reviewed preview and confirmation path in this release. Evidence: out/func-strings.txt:2988; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab pipeline job list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Pipeline-job enumeration is not exposed as a standalone connector operation. Evidence: out/func-strings.txt:2928; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab pipeline run` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Creating a pipeline has no reviewed preview and confirmation path in this release. Evidence: out/func-strings.txt:2920; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab project search` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Fuzzy project search is not exposed; the connector resolves one exact project identity. Evidence: out/func-strings.txt:2972; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab release create` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | GitLab release creation is not exposed by the approved connector contract. Evidence: out/func-strings.txt:3003; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab release list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | GitLab release enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2951; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab release view` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | GitLab release detail is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2955; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab search code` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Repository code search and snippets are not exposed by the bounded connector contract. Evidence: out/func-strings.txt:2968; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab tag list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Repository tag enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2947; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab todo done` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | User to-do mutation is not part of the project-scoped connector contract. Evidence: out/func-strings.txt:3019; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab todo list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | User-scoped to-do enumeration is not part of the project-scoped connector contract. Evidence: out/func-strings.txt:3015; SUPER-CLI-ARCHITECTURE.md section 6.5 |

### no-equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli gitlab project create` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Project provisioning is a governance operation outside the approved bounded connector contract. Evidence: out/func-strings.txt:2981; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab project edit` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Broad project-settings mutation is deliberately excluded from the bounded connector contract. Evidence: out/func-strings.txt:2978; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab variable list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | CI/CD variable enumeration can expose protected configuration and is deliberately excluded. Evidence: out/func-strings.txt:3012; SUPER-CLI-ARCHITECTURE.md section 6.5 |
| `super-cli gitlab webhook list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Webhook endpoint enumeration is outside the bounded project-content contract. Evidence: out/func-strings.txt:3009; SUPER-CLI-ARCHITECTURE.md section 6.5 |

## Confluence

### equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli confluence page search` | `{brand} confluence page search --cql <cql>` | `confluence_search` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The bounded CQL search provides the same discovery outcome. Evidence: out/func-strings.txt:2632; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence space list` | `{brand} confluence space list` | `confluence_list_spaces` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The bounded space inventory provides the same read outcome. Evidence: out/func-strings.txt:2616; SUPER-CLI-ARCHITECTURE.md section 6.4 |

### renamed

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli confluence page child list` | `{brand} confluence page child-list <content-id>` | `confluence_list_children` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Child-page enumeration is exposed under the curated hyphenated leaf. Evidence: out/func-strings.txt:2640; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page comment list` | `{brand} confluence page comment-list <content-id>` | `confluence_list_comments` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Bounded comments are exposed under the curated hyphenated leaf. Evidence: out/func-strings.txt:2664; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page view` | `{brand} confluence page get <content-id>` | `confluence_get_page` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The bounded page projection is exposed under the get spelling with an untrusted-content warning. Evidence: out/func-strings.txt:2636; SUPER-CLI-ARCHITECTURE.md sections 6.4 and 7.3 |

### safer-different

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli confluence page comment add` | `{brand} confluence page comment <content-id> --body-file <path> --dry-run\|--confirm` | `confluence_add_comment` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Comment input is bounded Markdown from a file and is escaped before storage rendering. Evidence: out/func-strings.txt:2667; SUPER-CLI-ARCHITECTURE.md sections 6.4 and 7.3 |
| `super-cli confluence page create` | `{brand} confluence page create <space-key> --title <text> --body-file <path> --dry-run\|--confirm` | `confluence_create_page` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Bounded Markdown is escaped into storage form and returned content remains marked untrusted. Evidence: out/func-strings.txt:2684; SUPER-CLI-ARCHITECTURE.md sections 6.4 and 7.3 |
| `super-cli confluence page update` | `{brand} confluence page update <content-id> --body-file <path> --dry-run\|--confirm` | `confluence_update_page` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Markdown is escaped into storage form and optimistic version conflicts remain explicit. Evidence: out/func-strings.txt:2687; SUPER-CLI-ARCHITECTURE.md section 6.4 |

### not-yet-supported

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli confluence page attachment list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Attachment enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2674; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page label add` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Label mutation is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2650; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page label list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Label enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2643; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page label remove` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Label mutation is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2654; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page version list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Page version history is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2658; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page version view` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Historical page-version bodies are not exposed by the approved connector contract. Evidence: out/func-strings.txt:2661; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence space view` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Standalone space-detail lookup is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2621; SUPER-CLI-ARCHITECTURE.md section 6.4 |

### no-equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli confluence page append` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Blind append is deliberately excluded because the bounded update path requires an explicit replacement body and version reconciliation. Evidence: out/func-strings.txt:2691; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page comment delete` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Comment deletion is deliberately excluded because no reviewed preview and reconciliation contract exists. Evidence: out/func-strings.txt:2670; SUPER-CLI-ARCHITECTURE.md section 6.4 |
| `super-cli confluence page delete` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Page deletion is deliberately excluded from the approved write contract despite source trash recoverability. Evidence: out/func-strings.txt:2680; SUPER-CLI-ARCHITECTURE.md sections 6.4 and 7.4 |
| `super-cli confluence page move` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Page moves are deliberately excluded because no bounded preview and hierarchy-reconciliation contract exists. Evidence: out/func-strings.txt:2677; SUPER-CLI-ARCHITECTURE.md section 6.4 |

## ARM

### equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli arm artifact info` | `{brand} arm artifact info <repo> <path>` | `arm_artifact_info` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The connector returns bounded metadata, checksums, and folder children. Evidence: out/func-strings.txt:2527; SUPER-CLI-ARCHITECTURE.md section 6.6 |

### renamed

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli arm artifact properties get` | `{brand} arm artifact properties <repo> <path>` | `arm_get_properties` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Bounded property reads use the curated properties leaf and optional repeatable keys. Evidence: out/func-strings.txt:2552; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm repo list` | `{brand} arm repository list` | `arm_list_repositories` | Pinned evidence asserts the bare command only. → Use the bounded operands and options shown in the replacement; do not infer source flags. | The replacement returns bounded normalized connector data rather than a raw vendor response. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Repository enumeration is exposed under the full repository noun with bounded filters. Evidence: out/func-strings.txt:2522; SUPER-CLI-ARCHITECTURE.md section 6.6 |

### safer-different

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli arm artifact delete` | `{brand} arm artifact delete <repo> <path> --dry-run\|--confirm` | `arm_delete` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Deletion requires exact intent, file-path confinement, and ambiguous-write refusal. Evidence: out/func-strings.txt:2542; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm artifact deploy` | `{brand} arm artifact deploy <repo> <path> --file <path> --dry-run\|--confirm` | `arm_deploy` | Source mutation operands evidenced by the command family. → Use the bounded replacement operands and file inputs shown.; Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output.; Mutation intent. → Supply exactly one of --dry-run and --confirm. | The replacement returns a bounded normalized preview/result and never an arbitrary raw vendor payload. Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | Deployment confines the local file, validates certificates, compares checksums first, and falls back to full upload when needed. Evidence: out/func-strings.txt:2538; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm artifact list` | `{brand} arm artifact info <repo> <path> --max-children <count>` | `arm_artifact_info` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | Folder listing is a bounded child projection of artifact metadata rather than an unbounded standalone list. Evidence: out/func-strings.txt:2531; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm search aql` | `{brand} arm artifact search --query-file <path>` | `arm_search_artifacts` | Source --url, credential, --raw, and --no-throttle escape hatches, if present in a script. → No argv translation; use active-profile configuration and bounded output. | The replacement is bounded and normalized and never returns an arbitrary raw vendor payload. Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | AQL is bounded, parsed locally from a file or one query option, and constrained to approved include fields and limits. Evidence: out/func-strings.txt:2567; SUPER-CLI-ARCHITECTURE.md section 6.6 |

### not-yet-supported

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli arm docker tag list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this source command. No direct execution is exposed; no mutation authority is granted. | Docker tag enumeration is not exposed by the approved connector contract. Evidence: out/func-strings.txt:2563; SUPER-CLI-ARCHITECTURE.md section 6.6 |

### no-equivalent

| SuperCLI 0.14.1 command | Replacement | Operation | Flag migration | Output and write behavior | Rationale and evidence |
|---|---|---|---|---|---|
| `super-cli arm artifact copy` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Server-side artifact copy is outside the approved deploy/delete contract and has no reviewed reconciliation path. Evidence: out/func-strings.txt:2546; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm artifact download` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Binary download has no equivalent; use bounded artifact metadata and checksums to identify content, then use an approved file-transfer path. Evidence: out/func-strings.txt:2534; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm artifact move` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Server-side artifact move is outside the approved deploy/delete contract and has no reviewed reconciliation path. Evidence: out/func-strings.txt:2549; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm artifact properties delete` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Artifact property mutation is deliberately excluded from the read-only property contract. Evidence: out/func-strings.txt:2559; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm artifact properties set` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Artifact property mutation is deliberately excluded from the read-only property contract. Evidence: out/func-strings.txt:2555; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm permission list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Permission-target enumeration is an administrative operation outside the bounded artifact contract. Evidence: out/func-strings.txt:2582; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm permission view` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Permission-target details are administrative data outside the bounded artifact contract. Evidence: out/func-strings.txt:2592; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm storage info` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Instance-wide storage administration data is outside the bounded artifact contract. Evidence: out/func-strings.txt:2571; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm xray scan` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Starting Xray scans is outside the approved artifact metadata and deployment contract. Evidence: out/func-strings.txt:2607; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm xray summary` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Xray vulnerability data is not part of the approved ARM connector contract. Evidence: out/func-strings.txt:2600; SUPER-CLI-ARCHITECTURE.md section 6.6 |
| `super-cli arm xray violation list` | — | — | No flag translation; this operation is unavailable. | No Ericsson connector CLI result is available for this deliberately excluded operation. No direct execution is exposed; no mutation authority is granted. | Xray policy violations are not part of the approved ARM connector contract. Evidence: out/func-strings.txt:2604; SUPER-CLI-ARCHITECTURE.md section 6.6 |

## Ericsson-only new capabilities

| Replacement | Operation | Access and write behavior | Why it is listed separately |
|---|---|---|---|
| `{brand} jira issue mine` | `jira_my_tickets` | read: Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The Ericsson connector adds an active-user ticket view without an exact pinned source command. |
| `{brand} gitlab commit comment-list <project> <sha>` | `gitlab_list_commit_comments` | read: Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The Ericsson connector adds bounded commit-comment discovery without an exact pinned source command. |
| `{brand} gitlab commit discussion-list <project> <sha>` | `gitlab_list_commit_discussions` | read: Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The Ericsson connector adds bounded commit-discussion discovery without an exact pinned source command. |
| `{brand} gitlab ci inspect <project>` | `gitlab_inspect_ci` | read: Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The Ericsson connector adds bounded multi-file CI inspection without an exact pinned source command. |
| `{brand} gitlab branch create <project> <branch> <ref> --dry-run\|--confirm` | `gitlab_create_named_branch` | write: Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | No exact pinned source command exists for explicit named-branch creation; the connector adds exact-ref identity reconciliation. |
| `{brand} gitlab branch create-ticket <project> <ticket-key> --summary <text> --dry-run\|--confirm` | `gitlab_create_branch` | write: Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | The Ericsson connector adds ticket-derived branch naming without an exact pinned source command. |
| `{brand} gitlab commit create <project> <branch> --commit-message <text> --change-file <path> --dry-run\|--confirm` | `gitlab_commit_changes` | write: Every write requires exactly one of --dry-run and --confirm; confirmed ambiguous outcomes return write_ambiguous and are never retried. | The Ericsson connector adds bounded multi-action commits without an exact pinned source command. |
| `{brand} confluence page body <content-id>` | `confluence_get_page_body` | read: Read-only; dry-run, confirmation, and ambiguous-write handling do not apply. | The Ericsson connector adds a bounded body-specific read without an exact pinned source command. |

## Script migration rules

1. Select the active OTTO or LOOP24 profile and enable the required
   standalone connector before execution.
2. Translate only flags established by reviewed documentation. The pinned
   string inventory proves bare command names, not every historical flag.
3. Move large Jira/Confluence text and ARM local files to the documented
   bounded file options. Do not translate arbitrary raw JSON mutations.
4. Add exactly one of `--dry-run` or `--confirm` to every write.
5. Parse the v1 envelope in JSON mode and handle exit code 5 separately
   from ordinary connector failures.
