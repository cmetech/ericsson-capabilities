# Task 2 report: shared error taxonomy with remediation

## Implementation summary

Added the canonical `shared/ericsson_common/errors.py` taxonomy with:

- stable HTTP status categories via `category_for_status`;
- `RETRYABLE_STATUSES = frozenset({429, 502, 503, 504})`;
- operator remediation text via `remediation_for`;
- `ConnectorError` carrying `category`, optional `service`, optional `detail`, and derived `remediation`, while preserving `str(error) == category`.

Added the required tests and synchronized the canonical module into both generated consumer copies.

## TDD commands and results

RED command:

```text
. .venv/bin/activate && PYTHONPATH=shared pytest tests/test_shared_errors.py -q
```

Result: expected collection failure:

```text
E   ModuleNotFoundError: No module named 'ericsson_common.errors'
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
```

GREEN command:

```text
. .venv/bin/activate && PYTHONPATH=shared pytest tests/test_shared_errors.py -q
```

Result:

```text
..........                                                               [100%]
```

## Sync and drift verification

Ran the required sync command:

```text
. .venv/bin/activate && python scripts/sync_shared.py
```

Result:

```text
synced -> plugins/ericsson-jira/_common
synced -> plugins/ericsson-gitlab/_common
```

The brief's combined command omitted `PYTHONPATH=shared` for pytest, so its first attempt failed at test collection with `ModuleNotFoundError: No module named 'ericsson_common'`. With the repository's required import path supplied, drift and taxonomy tests passed:

```text
. .venv/bin/activate && PYTHONPATH=shared pytest tests/test_shared_sync.py tests/test_shared_errors.py -q
.................                                                        [100%]
```

`cmp` verified byte identity between the canonical module and both generated copies; `git diff --cached --check` reported no whitespace errors.

## Files changed

- `shared/ericsson_common/errors.py`
- `plugins/ericsson-jira/_common/errors.py` (generated by sync)
- `plugins/ericsson-gitlab/_common/errors.py` (generated by sync)
- `tests/test_shared_errors.py`

## Self-review

The implementation matches the task brief exactly, keeps `ConnectorError` internal to `_common`, preserves category-only exception strings, and does not alter connector-local error types. Generated copies were not hand-edited.

## Concerns

The combined sync/test command in the brief does not set `PYTHONPATH=shared`; the test suite cannot import the canonical package without that path. The corrected command passes all 17 tests. The brief says 11 taxonomy tests, but the supplied test file contains 10 tests; all supplied tests pass.
